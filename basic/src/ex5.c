// 求周长和面积
#include <stdio.h>

int main(void)
{
    int height = 7;
    int width = 5;

    printf("Perimeter of the rectangle = %i inches\n", (height + width) * 2);
    printf("Area of the rectangle = %i square inches\n", height * width);

    return 0;
}